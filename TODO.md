# ASTEROID FORGE リモートリポジトリ移行 ToDo

- [x] 1. Gitリポジトリの初期化とリモート設定
  - `git init -b main` によるローカルリポジトリ初期化
  - リモート `origin` (`https://github.com/t2k2pp/asteroid_forge.git`) の登録
  - `.gitignore` の整備 (.DS_Store等の一時ファイル除外)
  - 初回コミット作成およびリモートリポジトリへの初期Push

- [x] 2. ASTEROID FORGE プロジェクト資産一式の登録と検証
  - ゲーム本体 (HTML/CSS/JS/Vendor)、画像/ドキュメント (README/DESIGN/EVALUATION/docs)、テストコードの登録
  - テストスイート (`node tests/run-tests.mjs`) の実行検証
  - コミット作成およびリモートリポジトリへのPush

- [x] 3. ToDo完了記録とリモート同期検証
  - `TODO.md` の完了状態更新
  - コミット作成およびリモートリポジトリへのPush
  - リモートリポジトリの状態確認と完了確認

---

# ゲーム挙動補正・設計書更新の差分反映 ToDo

- [x] 1. 差分精査・コード量増減および機能削除の確認、ユニットテスト実行
- [x] 2. 自機旋回方向補正・ポーズ/クラフト完全停止・DESIGN.md 更新のコミットとmainへのPush
- [x] 3. ToDo完了記録の更新とmainへのPush

---

# ゲームパッド対応 (Xbox / Switch 配列切替) 実装 ToDo

- [x] 1. 設計書更新・永続化ロジック (padLayout: 'xbox'|'switch') 実装と単体テスト検証
  - `DESIGN.md` にコントローラー入力仕様および配列切り替え（Xbox/Switch）の仕様を追記
  - `js/storage.js` のスキーマ・サニタイズに `padLayout` を追加
  - `tests/run-tests.mjs` に `padLayout` の保存・サニタイズテストを追加・検証
  - コミット作成およびリモートリポジトリ（origin/main）へのPush
- [x] 2. Gamepad API 入力ポーリング・アクションマッピング実装
  - `js/game.js` にコントローラー入力（アナログスティック、十字キー、ボタン、トリガー）処理を実装
  - Xbox配列 / Switch配列のボタン配置切替ロジックの実装
  - クラフト・ポーズ・タイトル・ゲームオーバーのコントローラー操作対応
  - コミット作成およびリモートリポジトリ（origin/main）へのPush
- [x] 3. UI切替トグル・操作ガイドの追加とブラウザ実機検証 (Playwright)
  - `index.html` / `js/ui.js` / `css/style.css` にコントローラー配列切替ボタンを追加
  - ヘルプモーダルにゲームパッド操作説明（Xbox/Switch）を追加
  - コントローラー接続・切断時のトースト通知の追加
  - `tests/browser-verify.mjs` にゲームパッドエミュレーション検証を追加・実行
  - コミット作成およびリモートリポジトリ（origin/main）へのPush
- [x] 4. ToDo完了記録の更新と最終リモート同期
  - `TODO.md` の完了状態更新
  - コミット作成およびリモートリポジトリ（origin/main）へのPush

---

# マウス・タッチ操作対応 (追従旋回・タップ射撃・フィールド外クラフト停止) 実装 ToDo

- [x] 1. 設計書更新とポインター入力仕様の策定
  - `DESIGN.md` にマウス・タッチ操作仕様（ドラッグ追従旋回、タップ射撃、フィールド外タップによるクラフト画面呼び出し＆完全停止）を追記
  - コミット作成およびリモートリポジトリ（origin/main）へのPush
- [x] 2. 3D座標投影・ポインター入力（旋回追従・推進・射撃・フィールド外判定）の実装
  - `js/game.js` にスクリーン→ワールド平面（Y=1）レイキャスト交差計算を実装
  - フィールド内タップ/ドラッグ時の機首旋回追従・推進・射撃処理を実装
  - フィールド外タップ時のクラフト画面呼び出し（完全時間停止）およびクラフト外側タップによる閉じる処理を実装
  - コミット作成およびリモートリポジトリ（origin/main）へのPush
- [x] 3. モバイル・タッチUI最適化とPlaywrightブラウザ実機検証
  - `css/style.css` にキャンバスの `touch-action: none;` 等を追加
  - `index.html` のヘルプモーダルにマウス/タッチ操作案内を追記
  - `tests/browser-verify.mjs` にマウスクリック/ドラッグ旋回、フィールド外クリックでのクラフト停止のE2Eテストを追加・実行
  - コミット作成およびリモートリポジトリ（origin/main）へのPush
- [x] 4. ToDo完了記録の更新と最終リモート同期
  - `TODO.md` の完了状態更新
  - コミット作成およびリモートリポジトリ（origin/main）へのPush

---

# スラスター常時起動バグ修正 (クリック中のみ起動するよう入力ライフサイクル是正) 実装 ToDo

- [x] 1. 設計書更新と入力ライフサイクル仕様の明確化
  - `DESIGN.md` にスラスター起動タイミング（クリック/タッチ中のみ有効、未入力時は毎フレーム即座に解除）を明記
  - コミット作成およびリモートリポジトリ（origin/main）へのPush
- [x] 2. 入力評価・リセットライフサイクルの是正実装
  - `js/game.js` の入力処理をリファクタリングし、ゲームパッド未接続時も毎フレーム `G.input` が必ず再計算・リセットされる構造へ改修
  - ポインター解放時（pointerup/pointercancel）および未クリック時に `thrust` が確実に解除されるよう実装
  - コミット作成およびリモートリポジトリ（origin/main）へのPush
- [x] 3. ブラウザ実機検証 (Playwright) でのスラスター停止テスト追加・検証
  - `tests/browser-verify.mjs` にマウスクリック解放時にスラスターが即座に停止するテストを追加・検証
  - 全テストの実行と合格確認
  - コミット作成およびリモートリポジトリ（origin/main）へのPush
- [x] 4. ToDo完了記録の更新と最終リモート同期
  - `TODO.md` の完了状態更新
  - コミット作成およびリモートリポジトリ（origin/main）へのPush

---

# 資源のマップ外脱出防止 (スクリーンラップ・トーラスワープ対応) 実装 ToDo

- [x] 1. 設計書更新と資源スクリーンラップ仕様の策定
  - `DESIGN.md` に資源（Fe/Crドロップ）の画面端スクリーンラップ（反対側から出現）およびトーラス吸着仕様を追記
  - コミット作成およびリモートリポジトリ（origin/main）へのPush
- [x] 2. 資源スクリーンラップおよびトーラス吸着の実装
  - `js/game.js` のピックアップ更新ループ内で `wrapPos` を適用し、境界脱出時に反対側へワープ
  - ワープ時に微細なパーティクル/リングエフェクトを発生させて視認性を向上
  - `js/entities.js` の `Pickup.prototype.update` 内で、トーラス最短距離による磁石吸着ベクトル計算を適用
  - コミット作成およびリモートリポジトリ（origin/main）へのPush
- [x] 3. ブラウザ実機検証 (Playwright) での資源ワープテスト追加・検証
  - `tests/browser-verify.mjs` に、マップ境界を超えた資源が反対側へワープしアリーナ内に保持されることのE2Eテストを追加・検証
  - 全テストの実行と合格確認
  - コミット作成およびリモートリポジトリ（origin/main）へのPush
- [x] 4. ToDo完了記録の更新と最終リモート同期
  - `TODO.md` の完了状態更新
  - コミット作成およびリモートリポジトリ（origin/main）へのPush

---

# スマホ縦長画面向けレスポンシブカメラ対応 実装 ToDo

- [x] 1. 設計書更新とレスポンシブカメラズーム仕様の策定
  - `DESIGN.md` に画面アスペクト比（特にスマホ縦長向き）に応じたレスポンシブカメラズーム仕様を追記
  - コミット作成およびリモートリポジトリ（origin/main）へのPush
- [x] 2. レスポンシブカメラズームとリサイズ追従の実装
  - `js/game.js` の `updateCamera()` 関数を実装し、画面縦横比 `aspect < 1.18` に応じて注視点からカメラ距離を自動スケールバック (`scale = max(1.0, 1.18 / aspect)`)
  - `init()` および `onResize()` で `updateCamera()` を呼び出し、カメラシェイクとの連動（`G.camBase`）も保証
  - コミット作成およびリモートリポジトリ（origin/main）へのPush
- [x] 3. ブラウザ実機検証 (Playwright) でのスマホ縦長画面表示テスト追加・検証
  - `tests/browser-verify.mjs` に、モバイル縦長画面（390x844）でアリーナ全境界が画面内（NDC [-0.95, 0.95]）に確実に収まっていることのE2Eテストを追加・検証
  - 全テストの実行と合格確認
  - コミット作成およびリモートリポジトリ（origin/main）へのPush
- [x] 4. ToDo完了記録の更新と最終リモート同期
  - `TODO.md` の完了状態更新
  - コミット作成およびリモートリポジトリ（origin/main）へのPush

---

# 星空ビジュアル改善・カメラズーム時巨大矩形解消・UI日本語化 実装 ToDo

- [x] 1. 設計書更新と修正仕様の策定
  - `DESIGN.md` にスターフィールドの遠景天球配置・円形テクスチャ仕様およびUIテキストの正式日本語表記（「継続出撃」）を追記・反映
  - コミット作成およびリモートリポジトリ（origin/main）へのPush
- [x] 2. スターフィールドの遠景円形テクスチャ化とPC/スマホ両対応の星空密度の実装
  - `js/entities.js` の `buildStars()` を改修し、円形グラデーションテクスチャの適用、カメラ最大後退距離を超える遠景（r=600〜950u）配置、視界集中分布（2000個）、`sizeAttenuation: false`（ピクセル固定星）を実装
  - カメラズーム時に星が巨大矩形化する不具合を解消し、PC画面でも豊かな星空を表現
  - コミット作成およびリモートリポジトリ（origin/main）へのPush
- [x] 3. UIの「继续出撃」から「継続出撃」への正式日本語化
  - `index.html` のボタンラベルを「▶ 継続出撃 (保存済みから)」に修正
  - `tests/browser-verify.mjs` および関連ドキュメントの「继续出撃」表記を「継続出撃」に更新
  - コミット作成およびリモートリポジトリ（origin/main）へのPush
- [x] 4. ブラウザ実機検証 (Playwright) とスクリーンショット検証
  - `tests/browser-verify.mjs` の全テスト実行（43件以上合格、エラー0確認）
  - PCおよびスマホ縦長画面でのスクリーンショット撮影・目視検証（巨大矩形なし、星空が綺麗に表示、継続出撃ボタン正常）
  - コミット作成およびリモートリポジトリ（origin/main）へのPush
- [x] 5. ToDo完了記録の更新と最終リモート同期
  - `TODO.md` の完了状態更新
  - コミット作成およびリモートリポジトリ（origin/main）へのPush

---

# ゲームパッド攻撃時スラスター誤動作防止・推進/射撃完全独立化 実装 ToDo

- [x] 1. 設計書更新とToDo作成
  - `DESIGN.md` のゲームパッド操作マッピング表を改訂し、推進と射撃の完全独立仕様を明記
  - `TODO.md` に実装ToDoを策定
  - コミット作成およびリモートリポジトリ（origin/main）へのPush
- [x] 2. ゲームパッド推進・射撃入力判定の完全独立化実装
  - `js/game.js` の `pollGamepad` を修正し、`padThrust` から `isDown(7)`(RT) を完全に除外
  - Xbox/Switch両配列で推進（LT/LB/十字上/スティック上/下ボタン0）と射撃（RT/RB/左ボタン2/右ボタン1）の条件を厳密分離
  - 単体テスト（`node tests/run-tests.mjs`）の実行検証
  - コミット作成およびリモートリポジトリ（origin/main）へのPush
- [x] 3. ブラウザ実機検証 (Playwright) でのRT射撃時スラスター不稼働テスト追加・検証
  - `tests/browser-verify.mjs` にゲームパッドRT射撃時に `G.input.thrust === false` かつ `G.ship.thruster.visible === false` であることのアサーションを追加
  - Playwright による実機ブラウザテスト実行・全件合格確認（エラー0）
  - コミット作成およびリモートリポジトリ（origin/main）へのPush
- [x] 4. ToDo完了記録の更新と最終リモート同期
  - `TODO.md` の完了状態更新
  - コミット作成およびリモートリポジトリ（origin/main）へのPush

---

# CRAFT機能充実化 (オプション・回転シールド・装甲回復・武器換装) 実装 ToDo

- [x] 1. 設計書更新とToDo作成
  - `DESIGN.md` に新クラフト体系（オプション、オービットシールド、装甲自動修復、バルカン/レーザー換装）の仕様を策定・追記
  - `TODO.md` に実装ToDoを策定
  - コミット作成およびリモートリポジトリ（origin/main）へのPush
- [x] 2. 純ロジック (`js/logic.js`) & 永続化 (`js/storage.js`) の拡張と単体テスト
  - `RECIPES` に `orbitShield`, `repair`, `option` を追加
  - `wpnMode` ('vulcan' | 'laser') の武器ステータス計算、換装コスト計算を実装
  - `js/storage.js` に `wpnMode` と新規アップグレードの永続化・サニタイズ処理を追加
  - `tests/run-tests.mjs` に単体テストを追加・検証 (19 passed, 0 failed)
  - コミット作成およびリモートリポジトリ（origin/main）へのPush
- [x] 3. 武器換装（バルカン/レーザー貫通砲）＆弾丸貫通ロジックの実装
  - `Bullet` に貫通カウント (`pierce`) および直線ビーム外見・高速弾丸の追加
  - `js/game.js` の `tryFireShip` に直線貫通レーザー発射処理および衝突貫通判定 (`b.onHit`) を実装
  - 主兵装換装 (`swapWeapon`) 処理を実装
  - コミット作成およびリモートリポジトリ（origin/main）へのPush
- [x] 4. グラディウス風オプション（軌跡スネーク追従＆同時射撃）の実装
  - `OptionDrone` クラスを実装し、自機の過去軌跡をスネーク追従
  - 自機射撃時に全オプションから弾を同時発射する処理を実装
  - コミット作成およびリモートリポジトリ（origin/main）へのPush
- [x] 5. 2秒1周オービットシールド（敵弾ペナルティなし消滅）の実装
  - `OrbitBit` クラスを実装し、2.0秒/周（角速度 π rad/s）で自機周囲を公転
  - 敵弾（Raider弾）との衝突判定・ペナルティなし即時消滅＋防御エフェクトを実装
  - 隕石・敵機への接触微ダメージを実装
  - コミット作成およびリモートリポジトリ（origin/main）へのPush
- [x] 6. 船体装甲自動修復（ナノリペア）の実装
  - `repair` レベルに応じた装甲修復タイマー・HP回復処理および回復エフェクト/SFXを実装
  - コミット作成およびリモートリポジトリ（origin/main）へのPush
- [x] 7. クラフトベイUI拡張（換装トグル、新スロット、キーボード/ゲームパッド対応）の実装
  - `js/ui.js` および `css/style.css` を改修し、8系統のクラフト行・武器換装ボタンを表示
  - 数字キー 1〜8 および換装操作（クリック・ショートカット・ゲームパッド）に対応
  - コミット作成およびリモートリポジトリ（origin/main）へのPush
- [x] 8. E2E実機ブラウザ検証 (Playwright) テストの追加と総合動作検証
  - `tests/browser-verify.mjs` に新クラフト（オプション追従・同時射撃、オービットシールド周回・敵弾消滅、装甲修復、武器換装）の全自動検証を追加
  - Playwright による実機ブラウザテスト実行・全件合格確認（エラー0）
  - コミット作成およびリモートリポジトリ（origin/main）へのPush
- [x] 9. ToDo完了記録の更新と最終リモート同期
  - `TODO.md` の完了状態更新
  - コミット作成およびリモートリポジトリ（origin/main）へのPush

---

# 自動修復（ナノリペア）バランス適正化 (回復速度調整・被弾時タイマー開始) 実装 ToDo

- [x] 1. 設計書更新とToDo作成
  - `DESIGN.md` の自動修復仕様を更新（Lv1: 35秒 / Lv2: 24秒 / Lv3: 16秒で1メモリ回復、HP全快時被弾でタイマー開始）
  - `TODO.md` に実装ToDoを策定
  - コミット作成およびリモートリポジトリ（origin/main）へのPush
- [x] 2. 純ロジック (`js/logic.js`) & 単体テスト (`tests/run-tests.mjs`) の更新
  - `repairInterval` を `[null, 35, 24, 16]` に改訂
  - 単体テストの期待値を更新して実行・検証 (19 passed)
  - コミット作成およびリモートリポジトリ（origin/main）へのPush
- [x] 3. ゲーム内修復タイマー・被弾時開始ロジック (`js/game.js`) の実装
  - HP全快時はタイマーを待機値（`repairInt`）に保持し、被弾してHP減少した瞬間から満了時間（35s/24s/16s）カウントダウンが開始されるよう実装
  - 回復量は1メモリ（HP+1）を維持し、回復成功時にタイマーを再セット
  - コミット作成およびリモートリポジトリ（origin/main）へのPush
- [x] 4. E2E実機ブラウザ検証 (`tests/browser-verify.mjs`) の更新と検証
  - 新しい回復間隔（Lv2: 24秒）および1メモリ回復動作のPlaywright E2E検証を更新・実行
  - 全件合格確認（エラー0）
  - コミット作成およびリモートリポジトリ（origin/main）へのPush
- [ ] 5. ToDo完了記録の更新と最終リモート同期
  - `TODO.md` の完了状態更新
  - コミット作成およびリモートリポジトリ（origin/main）へのPush
